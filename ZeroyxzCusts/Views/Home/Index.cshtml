@model IEnumerable<ZeroyxzCusts.Models.Customer>

@{
    ViewBag.Title = "Index";
}

<div ng-app='Customers' ng-controller='CustomersController'>
       <!-- We can attach a click event even to a h element-->
    <h4 ng-click="logIt({{ customers }}">Load Customers</h4>
    <ul>
        
        <li ng-repeat="customer in customers track by customer.CustomerId">
            @*
                *******THIS WORKS FINE*******
                <div><p id="name">Name:{{ customer.Name }}</p>
                <p id="address">Address:{{ customer.Address }},{{ customer.City }}, {{ customer.County }}, {{ customer.PostCode }}</p>
                <p id="email">Email:{{ customer.Email }}</p>
                <p id="twitter">Twitter:{{ customer.Twitter }}</p>
                <h4 ng-click="removeCustomer()">Remove</h4></div>
            *@

            @*
                *******THIS WORKS FINE*******
                Using ng-include and a child view in an html file
                <div ng-include="'/Client/Views/CustomerView.html'"></div>
            *@

            <directive list="dummy" title="dummy"></directive>
            
        </li>
    </ul>
</div>

@*<form id="addCustomer">
    <fieldset>
        <legend>Add New Customer</legend>
        <ol>
            <li>
                <label for="Name">Name</label>
                <input type="text" name="Name"/>
            </li>
            <li>
                <label for="Address">Address</label>
                <input type="text" name="Address" />
            </li>
            <li>
                <label for="City">City</label>
                <input type="text" name="City"/>
            </li>
            <li>
                <label for="County">County</label>
                <input type="text" name="County" />
            </li>
            <li>
                <label for="PostCode">PostCode</label>
                <input type="text" name="PostCode" />
            </li>
            <li>
                <label for="Email">Email</label> 
                <input type="text" name="Email" />
            </li>
            <li>
                <label for="Twitter">Twitter</label>
                <input type="text" name="Twitter" />
            </li>
        </ol>
        <input type="submit" value="Add" />
    </fieldset>
</form>*@
